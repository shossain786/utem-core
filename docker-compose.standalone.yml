services:
  utem-core:
    build:
      context: .
      dockerfile: Dockerfile.standalone
    image: utem-core:latest
    container_name: utem-core
    ports:
      - "9090:8080"
    volumes:
      - utem-data:/app/data
    restart: unless-stopped

volumes:
  utem-data:
    driver: local
